---
title: Authoring MCP Server
description: >-
  Connect AI clients to Documentation.AI to read, edit, and publish your
  documentation, and see every tool the server provides.
---
## Overview

The Authoring MCP Server lets an MCP-compatible AI client work in your documentation project. It can read pages, write and reorganize content, update `documentation.json`, manage branches, and publish changes.

This is the server your **documentation team** connects to, and it uses your role in your Documentation.AI organization. To give readers AI access to your published docs, use the [Reader MCP Server](/ai/reader-mcp-server). To work over HTTP from your own scripts or CI/CD, use the [REST API](/api-reference/overview).

## How edits work

- Changes the agent makes appear in the web editor as unpublished changes, the same as a teammate's edits.
- Nothing goes live until the changes are published.
- Publishing a branch includes every unpublished change on it, including your teammates' edits.

<Callout kind="alert" collapsed="false">
  The server acts with your Documentation.AI permissions. Connect it only to AI clients you trust, and ask the agent to work on a separate branch when you want a review before anything goes live.
</Callout>

## Setup and authentication

Add the server URL to your MCP client, then sign in with your Documentation.AI account. No API keys are needed.

```text
https://api.documentation.ai/mcp
```

<Callout kind="info" collapsed="false">
  The earlier URL, `https://api.documentationai.app/mcp`, keeps working. If you switch to the new URL, sign in once more.
</Callout>

## Configure your MCP client

<Tabs>
  <Tab title="Claude Code" icon="terminal">
    Run this command in your terminal:

    ```bash
    claude mcp add --transport http documentation-ai https://api.documentation.ai/mcp
    ```

    Then run `/mcp` in Claude Code, select `documentation-ai`, and sign in when the browser opens.
  </Tab>

  <Tab title="Claude" icon="message-square">
    In Claude on the web or in Claude Desktop, open **Settings**, go to **Connectors**, and choose **Add custom connector**. Enter a name such as `Documentation.AI` and the server URL, then connect and sign in.

    Custom connector availability depends on your Claude plan.
  </Tab>

  <Tab title="Cursor" icon="mouse-pointer">
    Add the server to `.cursor/mcp.json` in your project, or to `~/.cursor/mcp.json` to use it in every project:

    ```json
    {
      "mcpServers": {
        "documentation-ai": {
          "url": "https://api.documentation.ai/mcp"
        }
      }
    }
    ```

    Open the MCP settings in Cursor and sign in when the server asks for it.
  </Tab>

  <Tab title="VS Code" icon="code">
    Add the server to `.vscode/mcp.json` in your workspace:

    ```json
    {
      "servers": {
        "documentation-ai": {
          "type": "http",
          "url": "https://api.documentation.ai/mcp"
        }
      }
    }
    ```

    Start the server from the MCP servers list and sign in when prompted.
  </Tab>

  <Tab title="Other clients" icon="plug">
    If your client supports remote MCP servers, give it the server URL and sign in when prompted.

    If your client can only run local servers, connect through `mcp-remote`:

    ```json
    {
      "mcpServers": {
        "documentation-ai": {
          "command": "npx",
          "args": ["mcp-remote", "https://api.documentation.ai/mcp"]
        }
      }
    }
    ```
  </Tab>
</Tabs>

## Choose a project and branch

Tell the agent which documentation project and branch to work on. It finds and selects the project with `list_projects` and `select_project`, and works on your live branch unless you name a different one.

## Example workflow

Describe the task to your AI client in plain language, and it calls the tools for you.

<Steps>
  <Step title="Choose where to work" icon="folder" title-type="p">
    Ask the agent to use your documentation project and to create a branch for the work, such as `update-api-guides`.
  </Step>

  <Step title="Make the changes" icon="edit" title-type="p">
    Ask for the changes you want. The agent reads the relevant pages and edits them.
  </Step>

  <Step title="Review" icon="eye" title-type="p">
    Ask the agent to summarize what changed, or review the changes in the editor's Changes panel.
  </Step>

  <Step title="Publish the branch" icon="upload" title-type="p">
    Ask the agent to publish. The changes are saved to the branch, and your live site is unchanged.
  </Step>

  <Step title="Go live" icon="rocket" title-type="p">
    When the branch is ready, ask the agent to merge it into your live branch.
  </Step>
</Steps>

<Callout kind="tip" collapsed="false">
  For small fixes, the agent can edit your live branch directly. The edits stay unpublished until you ask it to publish, so you can review them first.
</Callout>

## Tools reference

The server provides 32 tools. Read-only tools never change your documentation. Destructive tools can overwrite or discard content, including your teammates' unpublished edits, so most clients ask you to confirm them first.

| Tool                      | What it does                                                | Minimum role | Behavior        |
| ------------------------- | ----------------------------------------------------------- | ------------ | --------------- |
| `list_projects`           | Lists your organizations, documentation projects, and roles | Viewer       | Read-only       |
| `select_project`          | Sets the default project and branch                         | Viewer       | Session setting |
| `get_component_reference` | Returns MDX component syntax and examples                   | Viewer       | Read-only       |
| `get_config_reference`    | Returns the `documentation.json` settings reference         | Viewer       | Read-only       |
| `get_site_config`         | Returns `documentation.json`                                | Viewer       | Read-only       |
| `list_pages`              | Lists the files in your project                             | Viewer       | Read-only       |
| `get_page`                | Returns the content of a file                               | Viewer       | Read-only       |
| `search`                  | Searches your published documentation                       | Viewer       | Read-only       |
| `fetch`                   | Returns a page from a search result                         | Viewer       | Read-only       |
| `list_changes`            | Lists unpublished changes on a branch                       | Viewer       | Read-only       |
| `diff_page`               | Shows the unpublished changes to a file                     | Viewer       | Read-only       |
| `list_branches`           | Lists branches and shows which one is live                  | Viewer       | Read-only       |
| `list_pull_requests`      | Lists pull requests                                         | Viewer       | Read-only       |
| `list_media`              | Lists media files in your project                           | Viewer       | Read-only       |
| `search_media`            | Searches media by filename or alt text                      | Viewer       | Read-only       |
| `create_page`             | Creates a page or another file                              | Editor       | Makes changes   |
| `create_branch`           | Creates a branch                                            | Editor       | Makes changes   |
| `update_page`             | Replaces specific text in a page                            | Editor       | Destructive     |
| `rewrite_page`            | Replaces the full content of a page                         | Editor       | Destructive     |
| `update_site_config`      | Changes values in `documentation.json`                      | Editor       | Destructive     |
| `resolve_conflict`        | Saves the resolved version of a conflicting file            | Editor       | Destructive     |
| `move_page`               | Moves or renames a file                                     | Editor       | Destructive     |
| `delete_page`             | Deletes a file                                              | Editor       | Destructive     |
| `publish`                 | Publishes the unpublished changes on a branch               | Editor       | Destructive     |
| `revert_page`             | Discards unpublished changes to one file                    | Editor       | Destructive     |
| `revert_all_changes`      | Discards every unpublished change on a branch               | Editor       | Destructive     |
| `merge_branches`          | Merges one branch into another                              | Editor       | Destructive     |
| `delete_branch`           | Deletes a branch                                            | Editor       | Destructive     |
| `import_media`            | Imports files from public HTTPS URLs                       | Editor       | Makes changes   |
| `request_upload`          | Generates a short-lived upload link for local files         | Editor       | Makes changes   |
| `replace_media`           | Replaces a stored file from a URL, keeping the same address | Editor       | Destructive     |
| `delete_media`            | Deletes one or more media files                             | Admin        | Destructive     |

Most tools also accept `organizationId` and `documentationId` to work on a specific project, and `branch` to work on a specific branch. Tools that return lists accept `limit` and `cursor` to page through results.

### Projects

<Expandable title="list_projects" default-open="false">
  Lists the organizations and documentation projects you can access, with your role in each organization.

  **Parameters:** None
</Expandable>

<Expandable title="select_project" default-open="false">
  Sets the documentation project, and optionally the branch, that later calls use by default.

  <ParamField body="organizationId" param-type="string" required="true" deprecated="false" show-location="false">
    Organization ID from `list_projects`
  </ParamField>

  <ParamField body="documentationId" param-type="string" required="true" deprecated="false" show-location="false">
    Documentation project ID from `list_projects`
  </ParamField>

  <ParamField body="branch" param-type="string" required="false" deprecated="false" show-location="false">
    Branch to work on. Defaults to your live branch.
  </ParamField>
</Expandable>

### Authoring reference

<Expandable title="get_component_reference" default-open="false">
  Returns the syntax, attributes, and examples for Documentation.AI MDX components.

  <ParamField body="component" param-type="string" required="false" deprecated="false" show-location="false">
    Component to describe, such as `Callout`. Omit it for every component.
  </ParamField>
</Expandable>

<Expandable title="get_config_reference" default-open="false">
  Returns the reference for `documentation.json` settings.

  <ParamField body="section" param-type="string" required="false" deprecated="false" show-location="false">
    Setting to describe, such as `navigation` or `redirects`. Omit it for an overview of every setting.
  </ParamField>
</Expandable>

### Read content

<Expandable title="get_site_config" default-open="false">
  Returns your `documentation.json`, including navigation, branding, and site settings.

  **Parameters:** `branch` (optional)
</Expandable>

<Expandable title="list_pages" default-open="false">
  Lists the files in your documentation project.

  **Parameters:** `branch` (optional)
</Expandable>

<Expandable title="get_page" default-open="false">
  Returns the content of a file, including unpublished changes.

  <ParamField body="path" param-type="string" required="true" deprecated="false" show-location="false">
    File path including the extension, such as `guides/setup.mdx`
  </ParamField>
</Expandable>

<Expandable title="search" default-open="false">
  Searches your published documentation and returns matching pages with a title, link, and snippet.

  <ParamField body="query" param-type="string" required="true" deprecated="false" show-location="false">
    What to search for
  </ParamField>

  <ParamField body="limit" param-type="integer" required="false" deprecated="false" show-location="false">
    Maximum number of results, from 1 to 50. Defaults to 10.
  </ParamField>
</Expandable>

<Expandable title="fetch" default-open="false">
  Returns the content of a page from a search result.

  <ParamField body="id" param-type="string" required="true" deprecated="false" show-location="false">
    The `id` from a search result
  </ParamField>
</Expandable>

### Edit content

<Expandable title="create_page" default-open="false">
  Creates a page or another file, such as a stylesheet or snippet, and can add the page to the navigation.

  <ParamField body="path" param-type="string" required="true" deprecated="false" show-location="false">
    File path including the extension, such as `guides/webhooks.mdx`
  </ParamField>

  <ParamField body="content" param-type="string" required="true" deprecated="false" show-location="false">
    The file content. An MDX page starts with frontmatter that includes a `title`.
  </ParamField>

  <ParamField body="addToNav" param-type="object" required="false" deprecated="false" show-location="false">
    Where to add the page in the navigation, and the title to show there
  </ParamField>
</Expandable>

<Expandable title="update_page" default-open="false">
  Replaces specific text in a page.

  <ParamField body="path" param-type="string" required="true" deprecated="false" show-location="false">
    File path including the extension
  </ParamField>

  <ParamField body="edits" param-type="array" required="true" deprecated="false" show-location="false">
    The replacements to make. Each has the text to find, `old_string`, and the text to use instead, `new_string`.
  </ParamField>

  <ParamField body="replaceAll" param-type="boolean" required="false" deprecated="false" show-location="false">
    Replace every occurrence of the text. Defaults to `false`.
  </ParamField>
</Expandable>

<Expandable title="rewrite_page" default-open="false">
  Replaces the full content of a page.

  <ParamField body="path" param-type="string" required="true" deprecated="false" show-location="false">
    File path including the extension
  </ParamField>

  <ParamField body="content" param-type="string" required="true" deprecated="false" show-location="false">
    The new content of the page, including frontmatter
  </ParamField>
</Expandable>

<Expandable title="move_page" default-open="false">
  Moves or renames a file and updates its navigation entries. Add a redirect to keep links to the old URL working.

  <ParamField body="path" param-type="string" required="true" deprecated="false" show-location="false">
    Current file path including the extension
  </ParamField>

  <ParamField body="destination" param-type="string" required="true" deprecated="false" show-location="false">
    New file path including the extension
  </ParamField>
</Expandable>

<Expandable title="delete_page" default-open="false">
  Deletes a file and removes it from the navigation.

  <ParamField body="path" param-type="string" required="true" deprecated="false" show-location="false">
    File path including the extension
  </ParamField>
</Expandable>

<Expandable title="update_site_config" default-open="false">
  Changes values in `documentation.json`, such as navigation entries, colors, or redirects.

  <ParamField body="patches" param-type="array" required="true" deprecated="false" show-location="false">
    The changes to make. Each has an `op` (`add`, `replace`, or `remove`), the `path` of the setting, and a `value` for `add` and `replace`.
  </ParamField>
</Expandable>

### Review and publish

<Expandable title="list_changes" default-open="false">
  Lists the unpublished changes on a branch.

  **Parameters:** `branch` (optional)
</Expandable>

<Expandable title="diff_page" default-open="false">
  Shows the unpublished changes to a file.

  <ParamField body="path" param-type="string" required="true" deprecated="false" show-location="false">
    File path including the extension
  </ParamField>
</Expandable>

<Expandable title="publish" default-open="false">
  Publishes all unpublished changes on a branch. Publishing your live branch updates your live site.

  <ParamField body="commitMessage" param-type="string" required="true" deprecated="false" show-location="false">
    A short summary of the changes
  </ParamField>
</Expandable>

<Expandable title="resolve_conflict" default-open="false">
  Saves the resolved version of a file when publishing reports a conflict.

  <ParamField body="path" param-type="string" required="true" deprecated="false" show-location="false">
    File path from the conflict
  </ParamField>

  <ParamField body="content" param-type="string" required="true" deprecated="false" show-location="false">
    The resolved content of the file
  </ParamField>
</Expandable>

<Expandable title="revert_page" default-open="false">
  Discards the unpublished changes to one file and restores its published content. Your teammates' unpublished edits to that file are discarded too.

  <ParamField body="path" param-type="string" required="true" deprecated="false" show-location="false">
    File to restore, including the extension
  </ParamField>
</Expandable>

<Expandable title="revert_all_changes" default-open="false">
  Discards every unpublished change on a branch, across all files and all collaborators, and restores its published state. Check `list_changes` first to see what will be discarded.

  **Parameters:** `branch` (optional)
</Expandable>

### Branches

<Expandable title="list_branches" default-open="false">
  Lists your branches and shows which one is live.

  **Parameters:** None
</Expandable>

<Expandable title="list_pull_requests" default-open="false">
  Lists pull requests for your documentation.

  <ParamField body="state" param-type="string" required="false" deprecated="false" show-location="false">
    `open`, `closed`, or `all`. Defaults to `open`.
  </ParamField>
</Expandable>

<Expandable title="create_branch" default-open="false">
  Creates a branch.

  <ParamField body="branchName" param-type="string" required="true" deprecated="false" show-location="false">
    Name for the new branch
  </ParamField>

  <ParamField body="sourceBranch" param-type="string" required="false" deprecated="false" show-location="false">
    Branch to start from. Defaults to your live branch.
  </ParamField>
</Expandable>

<Expandable title="merge_branches" default-open="false">
  Merges one branch into another. Merging into your live branch updates your live site.

  <ParamField body="base" param-type="string" required="true" deprecated="false" show-location="false">
    Branch that receives the changes
  </ParamField>

  <ParamField body="head" param-type="string" required="true" deprecated="false" show-location="false">
    Branch the changes come from
  </ParamField>

  <ParamField body="commitMessage" param-type="string" required="false" deprecated="false" show-location="false">
    A short summary of the merge
  </ParamField>
</Expandable>

<Expandable title="delete_branch" default-open="false">
  Deletes a branch. Your live branch cannot be deleted.

  <ParamField body="branchName" param-type="string" required="true" deprecated="false" show-location="false">
    Branch to delete
  </ParamField>
</Expandable>

### Media

<Expandable title="list_media" default-open="false">
  Lists media files in your project with pagination, kind filtering, and date filtering. Each result includes the public URL and an MDX placement snippet.

  <ParamField body="limit" param-type="integer" required="false" deprecated="false" show-location="false">
    Maximum number of results per page. Defaults to 20.
  </ParamField>

  <ParamField body="cursor" param-type="string" required="false" deprecated="false" show-location="false">
    Pagination cursor from the previous page.
  </ParamField>

  <ParamField body="kind" param-type="string" required="false" deprecated="false" show-location="false">
    Filter by media kind, such as `image`, `video`, or `document`.
  </ParamField>

  <ParamField body="since" param-type="string" required="false" deprecated="false" show-location="false">
    Only return media added after this ISO 8601 timestamp.
  </ParamField>
</Expandable>

<Expandable title="search_media" default-open="false">
  Searches media by filename or alt text, with the same kind and date filters as `list_media`.

  <ParamField body="query" param-type="string" required="true" deprecated="false" show-location="false">
    What to search for in filenames and alt text
  </ParamField>

  <ParamField body="limit" param-type="integer" required="false" deprecated="false" show-location="false">
    Maximum number of results per page. Defaults to 20.
  </ParamField>

  <ParamField body="cursor" param-type="string" required="false" deprecated="false" show-location="false">
    Pagination cursor from the previous page.
  </ParamField>

  <ParamField body="kind" param-type="string" required="false" deprecated="false" show-location="false">
    Filter by media kind, such as `image`, `video`, or `document`.
  </ParamField>
</Expandable>

<Expandable title="import_media" default-open="false">
  Imports up to 10 files from public HTTPS URLs. The server fetches each file, validates it, and stores it with the same type, size, and quota checks as a dashboard upload. Each successful result includes the media ID, public URL, and an MDX snippet. The tool does not edit any page.

  <ParamField body="files" param-type="array" required="true" deprecated="false" show-location="false">
    One to ten files to import. Each has a `url` (required), and optional `name` and `altText`.
  </ParamField>
</Expandable>

<Expandable title="request_upload" default-open="false">
  Generates a short-lived upload link that lets someone upload local files directly into your media library. The link expires after 15 minutes. Give the link to the user, then ask them for the stored filenames after the upload finishes so the agent can find and use them.

  <ParamField body="note" param-type="string" required="false" deprecated="false" show-location="false">
    A short note shown on the upload page, up to 200 characters.
  </ParamField>
</Expandable>

<Expandable title="replace_media" default-open="false">
  Replaces a stored file from a public HTTPS URL while keeping the same public address. Pages using that address update on the next publish without any edits.

  <ParamField body="mediaId" param-type="string" required="true" deprecated="false" show-location="false">
    The ID of the media file to replace
  </ParamField>

  <ParamField body="url" param-type="string" required="true" deprecated="false" show-location="false">
    Public HTTPS URL of the replacement file
  </ParamField>
</Expandable>

<Expandable title="delete_media" default-open="false">
  Deletes one or more media files and reclaims storage. Returns the deleted IDs, any failures with reasons, and the total bytes reclaimed.

  <ParamField body="mediaIds" param-type="array" required="true" deprecated="false" show-location="false">
    One or more media IDs to delete.
  </ParamField>
</Expandable>

## Resources

The server also provides these reference resources:

| Resource             | URI                     | Description                                 |
| -------------------- | ----------------------- | ------------------------------------------- |
| Components Reference | `docs://components`     | MDX components with attributes and examples |
| Config Reference     | `docs://config-schema`  | The `documentation.json` settings reference |
| Project Config       | `docs://project/config` | Your project's current `documentation.json` |

## Role-based access

Your role in your Documentation.AI organization decides which tools you can use.

| Role       | Access                                 |
| ---------- | -------------------------------------- |
| **Viewer** | Read-only tools, plus `select_project` |
| **Editor** | All tools                              |
| **Admin**  | All tools                              |

If you want an AI client to read your published documentation without being able to change it, use the [Reader MCP Server](/ai/reader-mcp-server) instead.

## Troubleshooting

<Expandable title="Sign-in fails or the browser window never completes" default-open="false">
  Make sure you are signed in to the Documentation.AI dashboard in the same browser, then remove the server from your client and add it again. If your client cannot connect to remote servers, use the `mcp-remote` configuration under **Other clients**.
</Expandable>

<Expandable title="A tool says no documentation project is selected" default-open="false">
  Ask the agent to list your projects and select the one you want to work on.
</Expandable>

<Expandable title="An editing tool is refused" default-open="false">
  Editing, publishing, and branch changes require the editor or admin role. Ask an admin in your organization to update your role.
</Expandable>

<Expandable title="Publishing reports a conflict" default-open="false">
  Someone published changes to the same part of a page, so nothing was published. Ask the agent to resolve the conflict, then publish again.
</Expandable>

<Expandable title="Search does not find a new page" default-open="false">
  Search covers your published documentation, so new or unpublished pages do not appear yet. Ask the agent to list your pages instead.
</Expandable>

<Expandable title="New tools do not appear" default-open="false">
  Start a new conversation, or reconnect the server in your client.
</Expandable>

## Related resources

<Columns cols="2">
  <Card title="Reader MCP Server" href="/ai/reader-mcp-server" icon="book-open" cta="Open the read-only server guide" horizontal="false">

  </Card>

  <Card title="MCP Servers overview" href="/ai/mcp-servers" icon="terminal" cta="See server options and when to use them" horizontal="false">

  </Card>

  <Card title="Real-Time Collaboration" href="/write-and-publish/collaboration" icon="users" cta="See how shared editing works" horizontal="false">

  </Card>

  <Card title="Branches & Previews" href="/write-and-publish/branches-and-previews" icon="git-branch" cta="Learn branch workflows" horizontal="false">

  </Card>
</Columns>
